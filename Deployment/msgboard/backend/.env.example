# Copy to .env and fill in real values. Never commit .env; it is not rsynced.
# The password here must match the one you set for the 'msgboard' MySQL user (LAB-SHEET step 3).
DATABASE_URL="mysql://msgboard:CHANGE_ME@localhost:3306/msgboard"
PORT=3000
HOST=127.0.0.1
